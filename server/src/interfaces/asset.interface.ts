export default interface IAsset {
  thumnail: {
    cloudinaryId: string;
    cloudinaryUrl: string;
  };
  midReso: {
    cloudinaryId: string;
    cloudinaryUrl: string;
  };
  maxReso: {
    cloudinaryId: string;
    cloudinaryUrl: string;
  };
}
